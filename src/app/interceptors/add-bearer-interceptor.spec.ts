import { AddBearerInterceptor } from './add-bearer-interceptor';

describe('AddBearerInterceptor', () => {
  it('should create an instance', () => {
    expect(new AddBearerInterceptor()).toBeTruthy();
  });
});
