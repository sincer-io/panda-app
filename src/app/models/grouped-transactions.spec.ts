import { GroupedTransactions } from './grouped-transactions';

describe('GroupedTransactions', () => {
  it('should create an instance', () => {
    expect(new GroupedTransactions()).toBeTruthy();
  });
});
